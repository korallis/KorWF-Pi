# repo-basic

Fixture repository for issue #35 context-retrieval tests. `src/auth.ts`
implements password hashing; `src/utils.ts` is unrelated filler so a search
for "password hashing" has a clear right answer.
